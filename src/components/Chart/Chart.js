import React from "react";
const Chart = ({ data }) => {
  const { month, investment, sell, revenue } = data;
  return (
    <div className="col">
      <h2>chart</h2>
    </div>
  );
};

export default Chart;
